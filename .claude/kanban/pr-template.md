# PR: redesign(mud-`<name>`): align to AGE Design System

## Summary

Redesigns `mud-<name>` to align with the new AGE Design System. Source of truth: Figma node `<node-id>`. Driven by Cline Kanban card `<card-url>` and the `redesign-component` agent.

## Visual changes

- `<List token/CSS changes — e.g., background swap brand.default → brand.subtle, border-radius 4 → 8px, new size xs>`

## API changes

- `<List or "none">`
- For breaking changes: list affected callsites and confirm they were updated.

## Token deltas

| Token | Before | After | Reason |
|---|---|---|---|
| `--<component>-<element>-<property>-<state>` | `<old>` | `<new>` | `<Figma alignment>` |

## Subagent reports (from `parallel-aux-tasks`)

### Pixel-Perfect Verifier
- Max diff: `<X>%` (acceptance: < 0.5%)
- States verified: `<N>` (light + dark)
- Result: ✅ PASS / ⚠️ WARN / ❌ FAIL

### A11y Verifier (WCAG 2.1 AA)
- Critical violations: `<N>` (must be 0)
- Warnings: `<M>`
- Modes covered: light ✅ + dark ✅
- `yarn audit:contrast`: ✅ PASS / ❌ FAIL

### Story Writer
- Stories generated: `<N>` (Default, AllVariants, AllSizes, States, …)
- All render without console errors: ✅

### Test Writer
- Tests added: `<N>`
- Coverage delta: `<before>% → <after>%`
- Structural WCAG contract assertions: `<M>`
- All tests pass: ✅

### Integration Checker
- Callsites scanned: `<N>` (production + stories + tests + docs)
- Stale callsites: `<list or "none">`
- Exports verified: ✅
- Public-API types re-exported: ✅

## Before / After Screenshots

| State | Before | After | Figma |
|---|---|---|---|
| default light | `<link>` | `<link>` | `<link>` |
| default dark | `<link>` | `<link>` | `<link>` |
| hover light | `<link>` | `<link>` | `<link>` |
| hover dark | `<link>` | `<link>` | `<link>` |
| disabled | `<link>` | `<link>` | `<link>` |

Attach as PNG files in the PR description, or link to Storybook deploy preview if CI publishes one.

## Verification

- [x] `yarn lint` — PASS
- [x] `yarn test` — PASS (coverage delta documented above)
- [x] `yarn tokens.build` — PASS
- [x] `yarn build` — PASS
- [x] `yarn sp.build` — PASS
- [x] `yarn audit:contrast` — PASS
- [x] Storybook a11y panel — 0 violations both modes
- [x] No console errors in any story

## Reviewer checklist

- [ ] Pixel-perfect diffs match Figma intent
- [ ] No accidental visual regressions on other components (sanity check `mud-button`, `mud-input` if shared tokens changed)
- [ ] No `--palette-*` usage in updated CSS
- [ ] CSS pattern preserved (A: slotted / B: internal DOM) — or breaking change approved separately
- [ ] Tests cover new variants/sizes/states introduced
- [ ] No `console.log` left in code

## Out of scope

- `<List anything deferred to a follow-up PR>`

## Kanban card

`<https://kanban.cline.bot/cards/...>`

---

*Generated by `redesign-component` subagent v1. See `.claude/agents/redesign-component.md` for the pipeline definition.*
