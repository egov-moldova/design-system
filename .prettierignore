# Directories
dist/
www/
loader/
storybook-static/
src/legacy/
scripts/__fixtures__/

# File Types
*.md

# Files
src/components.d.ts
custom-elements.json
.storybook/stories/assets/core.tokens.json

# Deployment and CI definitions — Azure Pipelines YAML and Helm charts. They
# are maintained by whoever runs the infrastructure, not by this repo's JS
# toolchain, and Prettier reformatting them produces review noise in files no
# component change ever touches. The chart templates could not be formatted
# anyway: Prettier's YAML parser cannot read Go templating and errors on every
# one of them, and their `{{- ... -}}` whitespace chomping is load-bearing.
azure/
/pipline-mud-publish-npm.yml

# Hand-maintained HTML pages — the integration walkthrough and the CDN demo.
# Neither is a build input, and Prettier rewrites prose it has no business
# rewrapping.
/INTEGRATION.html
web-components/CDN_TEST.html

# Generated by scripts/icons/build-registry.mjs
src/components/mud-icon/assets/icons.manifest.json
scripts/icons/figma-extraction-plan.json
scripts/icons/asset-urls.json
