/**
 * Label size variants
 */
export enum LabelSize {
  MD = 'md',
  SM = 'sm',
}

/**
 * Label state variants
 */
export enum LabelState {
  DEFAULT = 'default',
  HOVER = 'hover',
  ACTIVE = 'active',
  DISABLED = 'disabled',
  ERROR = 'error',
}
