export enum CorTimelineVariant {
  ON_PAGE = 'on-page',
  ON_MAP = 'on-map',
  ON_CARD = 'on-card',
  ON_MAP_SMALL = 'on-map-small',
}

export enum CorTimelineScaleType {
  MONTHS = 'months',
  YEARS = 'years',
}

export enum CorTimelineSelectorType {
  SINGLE = 'single',
  RANGE = 'range',
}
