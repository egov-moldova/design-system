export enum InputSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}

export enum InputLabelPosition {
  INSIDE = 'inside',
  OUTSIDE = 'outside',
}

export enum InputType {
  TEXT = 'text',
  EMAIL = 'email',
  PASSWORD = 'password',
}
