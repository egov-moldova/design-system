export enum LinkSize {
  MD = 'md',
  SM = 'sm',
}

export enum LinkState {
  DEFAULT = 'default',
  HOVER = 'hover',
  PRESSED = 'pressed',
  FOCUS = 'focus',
  DISABLED = 'disabled',
  ERROR = 'error',
}

export enum LinkUnderline {
  ALWAYS = 'always',
  HOVER = 'hover',
  NONE = 'none',
}
