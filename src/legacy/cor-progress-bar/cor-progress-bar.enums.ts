/**
 * Type variants for cor-progress-bar.
 */
export enum ProgressBarType {
  DEFAULT = 'default',
  ERROR = 'error',
}

/**
 * Size variants for cor-progress-bar.
 */
export enum ProgressBarSize {
  LG = 'lg',
  MD = 'md',
  SM = 'sm',
}
