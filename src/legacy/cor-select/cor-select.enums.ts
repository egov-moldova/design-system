export enum SelectSize {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
}

/**
 * Where the dropdown listbox opens relative to the trigger.
 *
 * `auto` lets the component flip to whichever side has more room.
 */
export enum SelectListPosition {
  AUTO = 'auto',
  TOP = 'top',
  BOTTOM = 'bottom',
}
