export enum TextareaLabelPosition {
  INSIDE = 'inside',
  OUTSIDE = 'outside',
}

export enum TextareaResize {
  NONE = 'none',
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
  BOTH = 'both',
}
