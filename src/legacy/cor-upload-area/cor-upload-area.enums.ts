/**
 * Whether the upload area accepts a single file or multiple files.
 */
export enum UploadVariant {
  SINGLE = 'single',
  MULTIPLE = 'multiple',
}

/**
 * Layout style for the upload area.
 */
export enum UploadStyle {
  REGULAR = 'regular',
  COMPACT = 'compact',
}
